"use strict";
describe("Data type", function () {
    it("should must declare", function () {
        let name = "Lukman Hakim";
        let balance = 1000000;
        let isVip = true;
        console.info(name);
        console.info(balance);
        console.info(isVip);
        // name = 100;
        // balance = "Lukman";
        // isVip = "Hakim";
    });
});
